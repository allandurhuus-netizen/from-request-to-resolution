# From sheet to signature

An offline, keyboard-driven presentation about the `contract-approve` automation.

Right and Left move between sections. Down and Up move between the views inside one.
`F` is full screen. Nothing is fetched after the page loads.

**Generated. Do not edit here.** This repository is assembled by `_system/build-site.py`
in the workspace that authors the deck, and anything changed here is overwritten on the
next build.
